MY Events Room Planner
======================

myevents-planner.html  - the whole site in one file (logo and favicon are embedded). Double-click to run it locally,
                         or upload this single file to any static host (GitHub Pages, Netlify, Cloudflare Pages).
assets/                - the logo files extracted from "MY Events Terms.pdf", plus the favicon, in case they are needed separately.

Live copy (claude.ai): https://claude.ai/artifact/Uc4uGRCM28gSSdJqPahfss

Notes
- On claude.ai, projects are saved to the site's own storage and sync across your devices.
- Anywhere else (local file / your own host), projects are saved in the browser only.
- This copy is the standalone build (full HTML document). The claude.ai copy is the same page without the outer wrapper.